name: filtercake
markdown: redcarpet
highlighter: true
permalink: :title/
baseurl:
url: http://filtercake.com

# sass:
#   style: compressed

exclude:
  - Procfile
  - render-linklist
  - render-tooling
  - Gemfile
  - Gemfile.lock
  - NOTES.md
  - Procfile
  - README.md
  - .travis.yml
  - deploy-travis.sh
  - deploy.sh

# doesn't solve it: _includes is only processed when copied into _site ¯\_(ツ)_/¯
# include:
#   - _includes